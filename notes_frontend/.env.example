# PUBLIC ENVIRONMENT VARIABLES
# Prefix with NEXT_PUBLIC_ for variables exposed to the browser.

# Base URL for API calls (future backend). When not provided, the app uses localStorage mock API.
NEXT_PUBLIC_API_BASE_URL=

# Application site URL (used for email redirect in future auth integration)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Feature flags (for future toggles)
NEXT_PUBLIC_ENABLE_MOCK_API=true
